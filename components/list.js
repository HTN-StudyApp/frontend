import Link from "next/link";

export default function List({}) {
  return (
    <div
      style={{
        minWidth: "125px",
        height: "90px",
        margin: "5px",
        marginRight: "25px",
        marginLeft: "5px",
      }}
      className="rounded-lg bg-purple-700"
    ></div>
  );
}
